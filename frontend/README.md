# Discography project Task (Frontend)

## Requirements

backend api should be up and running

## Install and Running
```bash
gem install bundler jekyll
cd frontend
bundle exec jekyll serve
```

## How use

goes to http://localhost:4000